
\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[margin=1in]{geometry}
\usepackage{biocon}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{natbib}

\graphicspath{{images/}}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black,
}


% notation
\newcommand{\modality}{M}
\newcommand{\feats}{F}
\newcommand{\estfeats}{\hat{\feats}}
\newcommand{\moddata}[1][\modality]{Data_{#1}}

\newcommand{\filt}{\mathrm{filter}}
\newcommand{\fwd}{\mathrm{forward}}
\newcommand{\inv}{\mathrm{inverse}}
\newcommand{\Mone}{M_1}
\newcommand{\Mtwo}{M_2}
\newcommand{\MA}{A}
\newcommand{\MB}{B}

\newanimal{ra}{genus=Rousettus,epithet=aegyptiacus}
\newanimal{mm}{genus=Micronycteris,epithet=microtis}
\newanimal{pd}{genus=Phyllostomus,epithet=discolor}
\newanimal{ef}{genus=Eptesicus,epithet=fuscus}
\newanimal{tb}{genus=Tadarida,epithet=brasiliensis}
\newanimal{gs}{genus=Glossophaga,epithet=soricina}

\title{Vicarious sonar learning in a bat-inspired robot}
\author{Dieter Vanderelst}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
\noindent\textit{[Abstract to be written once the framework and results sections settle.]}
\end{abstract}


\section{Introduction}

% Par 1
Sensory modalities (e.g., vision, olfaction, hearing, echolocation) differ greatly in what they allow an animal to perceive about its environment. Each modality encodes different environmental features \citep[e.g.,][]{Boonman2013}. Bat echolocation, for example, carries the distance to the nearest reflecting surface faithfully \citep{Kothari2018}, the spatial layout of complex scenes only lossily \citep{Vanderelst2016}, and colour not at all. Vision, in contrast, carries colour and the environment's two-dimensional layout faithfully.

Despite their differences, different modalities can carry information about the same feature. Their access to the world overlaps, allowing animals to encode world features through more than one sensory chain. Bottlenose dolphins, for example, recognise object features across echolocation and vision \citep{Herman1998,Harley2003,Bruck2022}. Rodents access spatial and shape features of nearby objects through both whiskers and vision \citep{Winters2010,Nikbakht2018,Guyoton2025}. Crucially, when modalities carry the same feature, they can differ in how accessible it is and how much computational effort is needed to recover and represent it. For example, for vision, the azimuth and elevation of objects is easility recoverable because of the retinoptopical encoding. Distance, on the other hand, is recoverable only approximately from cues such as disparity, motion parallax, and occlusion. Likewise, in hearing, the azimuth and elevation of a sound source requires computation to recover. 

The function that recovers a feature from a modality's sensory data is often called the modality's \emph{inverse model}. In animals, it is what the modality's neural pathways implement to turn sensation into perception and representation. The complexity of this inverse depends on the specific combination of modality and feature, not on the modality alone. For some combinations the inverse is trivial, or at least directly available from the modality's encoding. In bats, for instance, the auditory pathway and cortical circuits filter and transform raw echo data to explicitly represent the distance to the nearest object \citep{Kothari2018,Suga1990}. Similarly, vision's retinotopic organisation makes object azimuth directly readable from the location of activity in the visual pathway. For other combinations the inverse must be learned. The binaural signal carries source azimuth, but its extraction requires neural circuitry that compares interaural time and intensity differences \citep{Grothe2003}. Similarly, vision carries information about depth, but its extraction requires specialised circuitry that combines binocular disparity, motion parallax, and related cues \citep{CummingDeAngelis2001,Parker2007}. The same modality can therefore appear on both sides of the asymmetry, depending on which feature is considered.

The overlap, but asymmetry, in feature encoding across modalities gives rise to a learning opportunity. If one modality already extracts a given feature, it can supply training labels for another modality whose chain carries the same feature but does not yet extract and represent it. Barn owls are a well-known example. In these birds, vision calibrates the auditory map of space, and prisms that displace the visual map induce a matching displacement of the auditory map \citep{Knudsen1995}. The auditory pathway carries the spatial information but the inverse model is lacking. What vision supplies is the supervisory signal that labels auditory patterns with world locations, allowing the the auditory inverse model to be learned. Once trained, the auditory pathway can run the inverse model on its own mapping sound to space, and the owl can hunt in darkness. The same kind of cross-modal calibration is documented in mammals. The auditory space map in ferret superior colliculus develops normally only when matched visual experience is available \citep{KingCarlile1995}, and adult primate auditory localization can be recalibrated by sustained audio-visual spatial conflict \citep{Recanzone1998}. Interestingly, most documented cases of cross-modal calibration involve vision in the teaching role, perhaps reflecting vision's high spatial resolution and broad feature coverage \citep{MuganMacIver2020}. The framework for crossmodal learning of inverse modela, however, applies wherever the asymmetry of inverse difficulty holds.

Bats provide a particularly interesting case for cross-modal learning of inverse models. They have two sensory modalities, vision and echolocation, that both carry information about space but operate under sharply different environmental constraints \citep[e.g.,][]{Holland2005} [C: there must be more references for this very basic point.]. Echolocating bats operate in highly cluttered, spatially demanding environments: they navigate dense vegetation, return to specific roosts and feeding sites, and follow stable flight routes between them \citep{Holderied2006,Schnitzler2003,Barchi2013}. While echolocation is sometimes described as ``seeing with sound'' \citep[e.g.,][]{Moss2001,Simmons2012,Ulanovsky2008,Clare2015,Geipel2013}, it likely does not yield the rich, image-like information this metaphor implies. Indeed, for reasons we explained in detail elsewhere \citep[e.g.,][]{Vanderelst2015,Vanderelst2016,Steckel2013,Mansour2019,Mansour2019a}, sonar is poor at recovering the structure and layout of complex scenes. Natural objects return cascades of overlapping echoes \citep{Yovel2009,Vanderelst2016}, and the bat cochlea integrates these into responses that no longer resolve the individual reflectors \citep{Wiegrebe1996,Wiegrebe2008}. Behavioural and neural evidence both indicate that closely-spaced objects in cluttered scenes are perceived as single extended stimuli \citep{Geberl2019,Warnecke2018}. It follows that the information needed to reconstruct a 3D scene from sonar alone is, for natural environments, largely absent. Yet bats display remarkable spatial sonar-based competence, including stable idiosyncratic flight paths in dark cages \citep{Barchi2013}, centimetre-precision memory for feeder locations \citep{VonHelversen2005}, and successful homing after displacement \citep{Stones1969}.

Echolocating bats also see. Most echolocating species have functional vision \citep{Boonman2013,Hoffmann2019}, with behaviourally measured visual acuity in microchiropterans roughly comparable to that of similarly sized small mammals such as rats and deer mice \citep{ManskeSchmidt1976,BellFenton1986,Cechetto2020}. Their luminance sensitivity is within an order of magnitude of that of mice, sufficient for navigation under starlight \citep{Cechetto2023}. Therefore, the cross-modal calibration mechanism described above suggests how vision could supply spatial labels for learning an inverse model for specific features carried by but not easily extracted from sonar. A bat could learn this inverse model while having access to both vision and sonar, e.g., while exploring environments not in complete darkness. Indeed, many bats emerge from their roosts at dusk and forage under intermediate light levels of 1--10\,lux \citep{Boonman2013}, echolocate even in bright light \citep{Holland2005,Eitan2022}, modify their echolocation in response to available visual cues \citep{McGowanKloepper2020}, and preferentially recruit visual cues for prey detection when these are available \citep{EklofJones2003,Holland2009}.

The overlap in feature encoding across modalities also gives rise to a second opportunity. Some sensory modalties, e.g., vision, that provides access to a rich set of environmental features might give rise to a mental representation of an environment, or even allow simulating hypotheticals [C4: Here, we are somewhat going out on a limb. Perhaps this is where we bring in references to Mciver or the Bennet book as, to be honest, we have no evidence that bats can maintain a visual representation of the world. On the other hand, there might be some evidence from other animals and it might be a big step on the intelligence ladder found in mammals. In other words, we need some big picture handwaving here, preferable rounded, though].

A bat with a visual representation of an environment and a inverse model for feature available to both sonar and vision, could engage in offline rehearsal of behaviours that will later be deployed by sonar alone. In other words, an inverse model for features shared across sonar and vision could support cross-modal vicarious learning, i.e., training a behaviour through one modality and deploying it through another.

Cross-modal vicarious learning would specifically benefit sensory modality with low informational throughput that are ill-suited to build an coherent environmental representation such as whisking, sonar, or tactiale sensinging. Effectively, the representation built using the richere sensory modality (e.g., vision) can be used to train the animal it its use of the more proximate sense.  

[c5: let's work on the stuff above. If we agree on this, the summary of the paper should be easy]

The present paper instantiates this idea on a physical robot. The robot carries onboard sonar in a bat-inspired configuration (two receivers flanking an emitter) and is observed externally by an overhead camera paired with an annotated arena map. Distances to the nearest walls in three azimuthal slices are computable for any pose by direct geometric construction from the map; these geometric labels supervise the training of an inverse model from sonar envelope to the same three slice distances. The scene model also supplies imagined slice distances at arbitrary poses along a desired path, paired with the actions of a path-following teacher, to train a navigation policy. At deployment, the overhead camera and the map are out of the loop, and the robot follows a path in an arena it has only seen, not pinged.


\section{Discussion}

% Par 11
For cross-modal learning of a given task to be observed in an experiment, the following conditions need to be met.
%
\begin{itemize}
\item[1] A set of features $F$ must be encoded in the sensory data of two modalities $A$ and $B$, one of which, say $A$, extracts and explicitly represents them.
\item[2] The agent must have learned an inverse model for these features $F$ allowing their extraction from the data of modality $B$ using the feature labels provided by modality $A$.
\item[3] The set of features $F$ must include features that are task-relevant, i.e., that allow the animal to complete the experimental task.
\item[4] The animal must have learned to use the task-relevant bimodally accessible features.
\end{itemize}
%
% Par 12
For bats, learning the inverse model for features encoded in both vision and sonar can happen developmentally through bimodal experience of environments. As pointed out in the introduction, bats often use sonar under conditions where vision is also available. These instances would provide the necessary training data and feature labels to train an inverse model for extracting vision-first features from sonar data (or vice versa). Alternatively, it might be possible for bats to learn an inverse model during the training for a given experimental setup, if the features required by the task are specific and idiosyncratic to the experiment.

% Par 13
In themselves, conditions 1 and 2 are not sufficient for cross-modal learning to be observed in experiments. For an experiment to show evidence of cross-modal learning, the task used to assess the bats' performance must also hinge on features that are cross-modally available. Moreover, the bat must also elect to use them. This is especially relevant for experiments where the bat is free to learn to complete the task using a range of features, for example, because it has access to both modalities at the time of learning. As we discuss next, these stringent criteria might make observing cross-modal learning difficult. Indeed, despite the potential adaptive value of cross-modal learning, convincing direct evidence is lacking \citep[see also][]{PageHofstede2021}.

% Par 14
\citet{Danilovich2019} report two experiments testing cross-modal learning using \animal{ra}. In the first experiment, bats learned to discriminate two shapes while having access to both vision and sonar. Next, they tested whether the bats could discriminate the objects using sonar alone. They found that the bats were unable to discriminate between the objects in the dark.
This failure to demonstrate cross-modal learning might have been due to several criteria not being satisfied. First, with additional training in darkness, only one out of four bats was eventually able to complete the task using sonar. This indicates that the task might have been hard to complete acoustically, perhaps because task-relevant (criterion 3) cross-modally available features were not available (criterion 1), preventing the bats from learning or using an inverse model (criterion 2). In addition, during training with vision available, bats might have learned to use features of the objects that are not cross-modally available, leading to a failure during the testing phase when those features were removed (criterion 4).

% Par 15
In a second experiment, \citet{Danilovich2019} trained \animal{ra} to discriminate between two textures using sonar. Next, they tested whether the bats could perform the task using vision. In a cleaner replication of the experiment, only one out of five bats managed this \citep[Round 2 in][]{Danilovich2019}. This limited cross-modal performance could again be explained by a failure to satisfy the criteria outlined above. Even if bats were able to perceive task-relevant features using both sonar and vision (criteria 1, 2 and 3), they might have learned to use these, except for the successful individual, during the training (criterion 4).

% Par 16
Similar limitations make findings of \citet{Holland2005} inconclusive. These authors trained \animal{ra} to find a hidden perch in light with sonar masked by ultrasonic noise, then tested in darkness with echolocation alone. Performance dropped and bats had to relearn finding the perch, which they were able to do. The authors interpreted the need to relearn as evidence of no transfer between the two sensory systems. However, as for the experiments reported by \citet{Danilovich2019}, experimental evidence supporting criteria 1, 2, 3 is lacking. Moreover, the experiment was constructed to invalidate criterion 4. By masking sonar during training, they might have biased the animals to use environmental features only available to vision during training.

% Par 17
Even positive evidence needs to be scrutinised. \citet{Finger2025} trained one group of \animal{ra} to land on a fixed perch in light and a second only in darkness. The light-trained group later landed in darkness, while the dark-only group failed across many trials, with three of four bats never contacting the perch at all. \citet{Finger2025} interpret these findings as evidence for some form of cross-modal learning since bats that were first trained using sonar and vision managed to later find the perch using sonar alone while the sonar-only group did not. However, the two of three pretrained bats required substantial additional training when restricted to only using sonar. Hence, in these animals cross-modal learning must have been limited.


%TO DO: introduce/weave this in. This is just a reminder for me for now?
While criteria 1-4 would result in cross-modal learning in an experiment, for cross-modal \emph{vicarious} learning, two additional conditions need to be satisfied.
%
\begin{itemize}
\item[5] The agent needs a mental representation of the required task features. This representation can be modality-specific or amodal.
\item[6] The agent needs to engage in mental rehearsal of the task in modality $B$, against the represented features.
\end{itemize} 


\bibliography{references}
\bibliographystyle{plainnat}

\end{document}
